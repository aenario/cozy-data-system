language: node_js
node_js:
    - "0.10"
services:
    - couchdb
env:
    global:
        - NODE_ENV=test
    matrix:
        - VERSION=0.8.9
        - VERSION=0.8.21
        - VERSION=0.8.18
        - VERSION=0.10
        - VERSION=0.8
before_install:
    - sudo sed -i -e 's/^Defaults\tsecure_path.*$//' /etc/sudoers
    - npm install n coffee-script -g
    - sudo n $VERSION
    - node --version
